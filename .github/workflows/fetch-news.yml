name: Fetch news

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    env:
      WHATSAPP_TOKEN: ${{ secrets.WHATSAPP_TOKEN }}
      WHATSAPP_PHONE_NUMBER_ID: ${{ secrets.WHATSAPP_PHONE_NUMBER_ID }}
      WHATSAPP_TO: ${{ secrets.WHATSAPP_TO }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: claude/landing-page-domain-esf6bn

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - run: npm install

      - run: npm run fetch-news

      # Só envia se o token estiver configurado nos secrets do repositório.
      # Sem ele o passo é pulado e a coleta segue funcionando normalmente.
      # (o contexto `secrets` não é permitido em `if`, por isso o teste é no env)
      - name: Send top story to WhatsApp
        if: env.WHATSAPP_TOKEN != ''
        run: npm run send-whatsapp

      - name: Commit updated news
        run: |
          git config user.name "gaglidom-news-bot"
          git config user.email "actions@users.noreply.github.com"
          git add news/news.json
          git diff --staged --quiet || git commit -m "Atualiza notícias"
          git push
